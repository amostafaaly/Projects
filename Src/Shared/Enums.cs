namespace Projects.Src.Shared;

public class Enums
{
    public enum Faculty { CS, Engineering, Arts, Business, Science }
    public enum StudentStatus { Active, Graduated, Withdrawn }
    public enum InstructorStatus { FullTime, PartTime }
    public enum StudentLevel { Undergraduate, Graduate }
}
